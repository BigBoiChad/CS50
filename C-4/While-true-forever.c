//While forever
#include <stdio.h>
#include <cs50.h>

int main(void)
{
    // while is used for Boolean expressions, it can tell whether the parentis is trune or false, if the experssion is ture then it run forever
    while (true)
    {
        printf("True\n");
    }
}
